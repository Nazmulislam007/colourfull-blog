import React from "react";
import classes from "../styles/Footer.module.css";

const Footer = () => {
  return (
    <div className={classes.footer}>
      <p className={classes.footer__text}>All right reserved</p>
    </div>
  );
};

export default Footer;
